"""Glyph QA engine: SVG rendering, scoring, and broken-glyph triage.

Packaged from the flat ``python/`` directory so the modules stay importable
as top-level names (``import shared``, ``import score_glyph``) both when run
directly (``python python/<script>.py``) and after installation.
"""

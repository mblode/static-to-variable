---
"static-to-variable": patch
---

Allow provenance-marked optical drawings to preserve an existing TrueType default master exactly. The optional `quadraticReference` style contract reconciles authored cubic masters to compatible quadratic topology before varLib, keeps the protected UI outline and metrics unchanged, and fails closed on incompatible geometry.

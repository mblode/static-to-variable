---
"static-to-variable": patch
---

Subdivide extra Text quadratic spans along the protected Display curve instead of collapsing them to stationary start prefixes, so intermediate optical sizes stay smooth and compiled Display stays native.

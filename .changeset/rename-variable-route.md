---
"static-to-variable": patch
---

Point the package homepage at `blode.co/static-to-variable`. The demo moved off `/variable` so the URL matches the package name; the old path is permanently redirected.

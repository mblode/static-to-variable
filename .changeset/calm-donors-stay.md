---
"static-to-variable": patch
---

Preserve compatible donor curves instead of rebuilding them, including exact contour-start rotation and compatible node insertion. This reduces reconstruction distortion while keeping cross-master topology aligned.
